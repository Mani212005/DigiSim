{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{
          "type": "command",
          "command": "cd /home/mani/digi/DigiSim/frontend && [ -f tsconfig.json ] && npx tsc --noEmit 2>&1 | tail -5 || true"
        }]
      }
    ]
  }
}
